global:
  resolve_timeout: 5m

route:
  receiver: default-webhook
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: default-webhook
    webhook_configs:
      - url: http://host.docker.internal:5001/alerts
        send_resolved: true
